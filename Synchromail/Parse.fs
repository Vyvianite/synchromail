namespace Synchromail

module Parse =

  ///Takes a list of emails and the rules to validate those emails, and returns the emails that should become tickets
  let parse (emails: Email seq) (rules: Rules array) : Email Seq =
    ()
